package com.example.fifa2026poolpredictions.theme

import androidx.compose.ui.graphics.Color

// ── Material theme seed colors ───────────────────────────────────────────────
val Green80     = Color(0xFFA5D6A7)
val GreenGrey80 = Color(0xFFB2DFDB)
val Gold80      = Color(0xFFFFE082)
val Green40     = Color(0xFF2E7D32)
val GreenGrey40 = Color(0xFF00695C)
val Gold40      = Color(0xFFF9A825)

// ── App green palette ────────────────────────────────────────────────────────
val AppGreenDark    = Color(0xFF166534)  // headings, selected labels, score text
val AppGreen        = Color(0xFF15803D)  // buttons, CTAs, winner highlights
val AppGreenBright  = Color(0xFF4ADE80)  // score badge border, active card border
val AppGreenLight   = Color(0xFFDCFCE7)  // score badge background, countdown border
val AppGreenSurface = Color(0xFFF0FDF4)  // faint green: selected cards, champion bg, step numbers
val AppGreenBorder  = Color(0xFF86EFAC)  // light green border: champion card, legend swatch

// ── Gray palette ─────────────────────────────────────────────────────────────
val Gray900 = Color(0xFF111827)  // near-black: selection names, result badge
val Gray700 = Color(0xFF374151)  // dark: team name body text
val Gray600 = Color(0xFF4B5563)  // medium: user names, draw badge, bracket headers
val Gray500 = Color(0xFF6B7280)  // secondary: subtitles, stats, empty states
val Gray400 = Color(0xFF9CA3AF)  // muted: table headers, dates, annotations
val Gray300 = Color(0xFFD1D5DB)  // bracket connector lines
val Gray200 = Color(0xFFE5E7EB)  // card borders, dividers
val Gray100 = Color(0xFFF3F4F6)  // upcoming badge background
val Gray50  = Color(0xFFF9FAFB)  // off-white surface: TBD cards, match rows

// ── Yellow palette ───────────────────────────────────────────────────────────
val YellowSurface = Color(0xFFFEFCE8)  // may-advance row background
val YellowLight   = Color(0xFFFEF08A)  // may-advance legend swatch
val Yellow        = Color(0xFFFDE047)  // may-advance legend border
